// Commands for creating, editing, and viewing tags
// Tags are keywords that map to useful information, e.g links, explanations, etc


//TODO! Should these return Result<>s?

//TODO! Decide on behavior for when a key already exists
// Should there be a seperate edit_tag(), or should it be handled by create_tag()?
fn create_tag(key: String, message: String) {
  todo!();
}

fn delete_tag(key: String) {
  todo!();
}

fn get_tag(key: String) -> String {
  todo!();
}
